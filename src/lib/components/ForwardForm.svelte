<!--
  ForwardForm — add/edit dialog (incl. keepalive fields + keychain warning).
  Password flows via set/clear_forward_password only, never in ForwardInput
  (spec 02 §6.1). Placeholder — implemented in M4. Visuals per 05-UI-UX-SPEC.md.
-->
