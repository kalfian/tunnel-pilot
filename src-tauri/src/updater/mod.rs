//! Self-updater wiring (`tauri-plugin-updater`, minisign-signed bundles):
//! check/download/verify/install + progress events (spec 03 §16).
//!
//! TODO(M6): check/install/skip; `update://status` + `update://progress`.
