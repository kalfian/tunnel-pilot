//! Backup commands (spec 02 §6.5): `export_backup` (strips passwords),
//! `import_backup` (validate, reject version>current, replace|merge).
//!
//! TODO(M4): implement against `storage/backup.rs`.
