//! Backup export (strip passwords) / import (validate, reject version>current,
//! replace|merge; lenient v1-backup import, F19) — spec 04 §11.
//!
//! TODO(M4): export/import + `ImportMode`/`ImportResult`.
