//! Atomic read-merge-write of `tunnel_pilot_config.json`; corruption →
//! `.corrupted` quarantine; single canonical `app_config_dir` (F2, spec 03 §7).
//!
//! TODO(M2): atomic write, read-merge-write, corruption handling.
