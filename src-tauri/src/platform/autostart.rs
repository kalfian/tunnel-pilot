//! Launch-at-login via `tauri-plugin-autostart`; reconcile with `launchAtLogin`
//! on every boot; autostarted launches open hidden (spec 03 §12, 06 F18).
//!
//! TODO(M3): sync plugin `is_enabled()` with the setting on boot.
