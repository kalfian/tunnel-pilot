//! Tray menu build + debounced rebuild-on-change: per-tunnel rows (Retry on
//! error), conditional bulk Start/Stop All, update-notice slot (spec 03 §13).
//!
//! TODO(M3): menu build/rebuild wired to engine commands.
