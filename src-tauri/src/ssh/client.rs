//! russh `Handler` impl + auth (password/identity, identity precedence) and
//! `keepalive_interval`/`keepalive_max` config (spec 03 §§1,2).
//!
//! TODO(M1): connect (15s timeout), auth (30s), keepalive config.
